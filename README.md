# tmux config

Personal tmux configuration managed with TPM. Prefix remapped to `C-Space`, gruvbox dark theme, vi copy-mode with xclip, vim-tmux-navigator for seamless pane navigation, tmux-sessionizer bound to `prefix+f`.
